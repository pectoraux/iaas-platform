{
  "//": "WORK-007 (BASE-012/BASE-013): Explicit TypeScript project boundary. The examples/ tree is auxiliary reference material, not part of the IAAS application compiler. It is excluded from the root tsconfig.json and has its own standalone configuration. The socket.io dependencies are not installed in the IAAS application; this tsconfig exists to declare the boundary explicitly rather than silently excluding broken code. The examples are not compiled in CI — they are reference material for developers.",
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "skipLibCheck": true
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
