# IAAS Architecture Specification

## Versions

| Artifact | Version | Status |
|---|---|---|
| Governance Architecture | `IAAS-GOV-ARCH-1` | FROZEN |
| Domain Architecture | `IAAS-DOM-ARCH-1` | FROZEN |

The canonical Domain Architecture V1 (`IAAS-DOM-ARCH-1`) is published in `spec/domain-architecture.md` and reconciles the existing `docs/architecture/` corpus (Constitution + Phase 13R reconciliation + Phase 14A–14F contracts) with actual repository evidence. The truth-classified audit is recorded in `docs/architecture/REPOSITORY-BASELINE.md` and `docs/architecture/RECONCILIATION-MATRIX.md`.

Domain architecture changes require an Architecture Change Request and a new version (`IAAS-DOM-ARCH-2`). The existing `docs/architecture/` corpus remains repository evidence; `IAAS-DOM-ARCH-1` is the canonical synthesis, not a replacement of the constitution.
