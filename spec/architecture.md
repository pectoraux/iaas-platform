# IAAS Architecture Specification

## Versions

| Artifact | Version | Status |
|---|---|---|
| Governance Architecture | `IAAS-GOV-ARCH-1` | FROZEN |
| Domain Architecture | `IAAS-DOM-ARCH-2` | FROZEN |
| Domain Architecture | `IAAS-DOM-ARCH-1` | SUPERSEDED |

`IAAS-DOM-ARCH-1` remains the immutable historical record produced by WORK-002.

The current canonical Domain Architecture is `IAAS-DOM-ARCH-2`, published in `spec/domain-architecture-v2.md` and frozen through `ACR-001`. V2 preserves V1 except for the explicit `VerifiedEvidenceContext` boundary.

The existing `docs/architecture/` corpus remains repository evidence; neither V1 nor V2 silently replaces the constitution. Domain architecture changes beyond V2 require a new Architecture Change Request and `IAAS-DOM-ARCH-3`.
