# Architecture Change Requests

Approved architecture changes must be reviewed by the Chief Architect, produce a new frozen architecture version, and leave the previous frozen version immutable.

Current active request:

- `ACR-003` — Extension Stack Architecture — `DRAFT`

`ACR-003` does not authorize production implementation or promote DOM-P04 by itself.
